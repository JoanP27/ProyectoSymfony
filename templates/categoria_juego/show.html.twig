{% extends 'base.html.twig' %}

{% block title %}CategoriaJuego{% endblock %}

{% block body %}
    <h1>CategoriaJuego</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categoria_juego.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ categoria_juego.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categoria_juego_index') }}">back to list</a>

    <a href="{{ path('app_categoria_juego_edit', {'id': categoria_juego.id}) }}">edit</a>

    {{ include('categoria_juego/_delete_form.html.twig') }}
{% endblock %}
